# 🔐 React Password Generator

A simple and customizable **password generator** built using **React.js**.  
It allows users to generate strong random passwords with options to include numbers and special characters, and easily copy them to the clipboard.

---

## 🚀 Features

- Adjust password **length** using a slider  
- Include or exclude **numbers** and **special characters**  
- One-click **Copy to Clipboard** functionality  
- Automatically generates a new password on every change  
- Responsive and modern UI using **Tailwind CSS**

---

## 🛠️ Tech Stack

- **React.js** (Vite)
- **Tailwind CSS**
- **JavaScript (ES6+)**
- **Clipboard API**

---